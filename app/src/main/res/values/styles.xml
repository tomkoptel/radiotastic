<resources>

    <style name="RadiotasticTheme" parent="RadiotasticTheme.Base">

    </style>

    <!-- Base application theme. -->
    <style name="RadiotasticTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="RadiotasticTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="RadiotasticTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
