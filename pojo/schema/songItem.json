{
    "description": "Represents song history item.",
    "type": "object",
    "properties": {
        "name": { "type": "string" },
        "title": { "type": "string" },
        "info": { "$ref": "songInfo.json"},
        "date": { "$ref": "songDate.json"}
    }
}